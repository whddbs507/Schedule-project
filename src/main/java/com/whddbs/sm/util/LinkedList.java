package com.whddbs.sm.util;

public class LinkedList {
  static class Node {
    Object value;
    Node next;
  }
  
  /*
   add(value)
   get(index)
   add(index, value)
   remove(index)
   set(index, value)
   toArray()
   size() 
    */
}
